name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    uses: eeekcct/goreleaser-golang-workflow/.github/workflows/release.yml@2d8ac3bb13bad083cc4bf8e8bd77a4ce14662c5a
    permissions:
      contents: write
      id-token: write
      actions: read
      attestations: write
